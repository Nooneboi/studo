# Learner resources

Place downloadable learner files here (PDF, DOCX, worksheets, etc.).
Register each published file in `content-src/resources/rla.resources.json` and attach it to one or more stable RLA skill IDs.
