# Uploading Studo to GitHub

This package has obsolete backup files removed so it stays under GitHub's browser upload count limit for the current version.

For ongoing development, prefer GitHub Desktop or Git from the command line instead of browser drag-and-drop, because the project will exceed 100 files again as resources are added.

Do not commit node_modules if it is created locally.
